# vividDeck 待开发功能清单

> 来源：v1.4.0 代码审查（2026-09-24），详见 `docs/代码审查报告-v1.4.0.md`。
> 完成一项请勾选并备注版本号。工作量：S ≤ 半天 / M ≤ 2 天 / L > 2 天。

## 一、面向用户功能

### 高优先级 —— 已实现能力的"半成品变现"（成本低、体验收益大）

- [ ] **1. 智能相册管理闭环**（S）
  侧栏相册支持右键 编辑/重命名/删除；创建相册后自动选中该相册。
  现状：`updateAlbum`/`deleteAlbum`/`AlbumEditorModal` 的 `editing` 分支代码已存在但 UI 不可达；创建后 `setFilter({albumId:null})` 反而清掉了筛选。
  涉及：`Sidebar.tsx`、`AlbumEditorModal.tsx`、`store/library.ts`、`ipc.ts`（`album:add` 需返回新 id）。

- [ ] **2. 相册标签交集（tagsAll）UI**（S）
  `SmartAlbumRules` 类型与文件头注释都承诺"标签交并集"，编辑器只实现了"任一命中"。补一个 交集/并集 切换。
  涉及：`AlbumEditorModal.tsx`、`shared/album.ts`（`matchAlbum` 双端共享，改一处即可）。

- [ ] **3. 画廊批量下载：进度 + 取消 + 失败重试**（S-M）
  现状：严格串行、不可取消、错误被吞（注释却写"支持取消"）；主进程已有并发 3 + 可取消的 `downloadScope` 未复用。
  方案：扩展 `downloadScope` 接受 id 列表，画廊走该通道，透出 `SYNC_PROGRESS` 进度，按钮加取消。
  涉及：`engine.ts`、`ipc.ts`、`preload`、`GalleryGrid.tsx`。

- [ ] **4. 同步区错误态 + 体检结果深链**（S）
  `getSyncInfo` 失败时整节渲染空白 → 补错误卡片 + 重试按钮；体检报告的"断链 N 张 / 缺失 N 张"支持点击直接套用画廊筛选查看。
  涉及：`SyncSection.tsx`、`store/library.ts`（filter 扩展）。

- [ ] **5. 轮播池为空告警**（S）
  分类/相册范围 poolSize 为 0 时静默不轮播（现在只对 全部/收藏 计算池大小）。补"该范围没有可用图片"提示。
  涉及：`SlideshowPanel.tsx`。

### 中优先级 —— 体验补强

- [ ] **6. 键盘驱动画廊**（M）
  方向键移动、Space 选中、Cmd/Ctrl+A 全选、Delete 打开删除框、Esc 逐级退出；右键菜单补 `role="menu"` 与 Esc 关闭。同时解决审查中最大的 a11y 缺口（虚拟网格当前完全键盘不可达）。
  涉及：`GalleryGrid.tsx`（建议与 A1 拆分一起做）。

- [ ] **7. 筛选/排序/浏览状态持久化**（S）
  重启后恢复上次的分类/标签/排序/滚动位置。
  涉及：`store/library.ts`、`store/ui.ts`、settings 存储。

- [ ] **8. 磁盘空间回收（缓存清理）**（S-M）
  体检报告增加"可清理 N GB"行：孤儿缩略图/预览（记录已被远端删除但缓存还在）、`applied/` 预渲染缓存 LRU 上限（当前无限增长，5000 图 × 2 屏可达 20GB+）、`.part.minio` 残留。
  涉及：`thumbnails.ts`、`wallpaper/prerender.ts`、`sync/health.ts`、`SyncSection.tsx`。

- [ ] **9. 选择性同步**（M）
  仅同步 收藏/指定分类/指定相册，控制多设备流量与磁盘占用；配合容量预估展示。
  涉及：`sync/engine.ts`、`shared/types.ts`（SyncConfig）、`SyncSection.tsx`。

- [ ] **10. 远端墓碑保险丝**（M）
  单次同步中"纯远端墓碑"导致的删除超过阈值（如 >10% 或 >N 条）时暂停合并、保留同步前状态并要求用户确认，防止一台被攻破设备清空全库。
  涉及：`sync/merge.ts`、`engine.ts`、`SyncSection.tsx`（确认 UI）。

### 长期

- [ ] **11. i18n 国际化**（L）
  全部 UI 字符串硬编码 zh-CN；若有出海计划，越晚抽取成本越高（建议 i18next + 按组件命名空间）。

## 二、工程基建

- [ ] **12. 测试框架（Vitest）**（M，随后持续补）
  当前覆盖率 0%。优先覆盖纯逻辑高价值区：`sync/merge.ts`（LWW/墓碑/同 hash 去重）、`shared/album.ts`（matchAlbum 组合语义）、虚拟滚动行数学、`sync/health.ts` 对账。目标逐步到 80%。

- [ ] **13. ESLint + Prettier + git hooks**（S）
  接入后可自动拦截本次审查中大量 MEDIUM（exhaustive-deps、floating promise、死代码）。推荐 hook 顺序：format → lint → typecheck → build。

- [ ] **14. 日志系统 + 崩溃兜底**（S）
  `electron-log` 落盘日志（同步问题远程排查刚需）；主进程补 `uncaughtException`/`unhandledRejection`/`render-process-gone` handler（当前全无）；日志中脱敏 endpoint/key。

- [ ] **15. React ErrorBoundary**（S）
  渲染进程任何组件抛错即白屏；补全局边界 + "重新加载"按钮，轮播面板/同步区等关键区块可加局部边界。

- [ ] **16. 自动更新**（M）
  `electron-updater` 未在依赖中；对外分发 NSIS/dmg 后无更新通道。需配套发布服务器（GitHub Releases 即可）。

## 三、架构演进（审查 P2/P3，工作量较大，单独立项）

- [ ] **A1. 拆分 `GalleryGrid.tsx`（987 行超限）**（M）
  → `components/gallery/`：`ImageCard`、`CardContextMenu`、`RenameModal`、`batch/BatchActionBar`、`batch/BatchTagModal`、`batch/BatchCategoryModal`、`batch/DeleteConfirmModal`（与 Lightbox 共享）；抽 `hooks/useVirtualGrid.ts` 与 `hooks/useUndoShortcuts.ts`（提到 App 层）。

- [ ] **A2. IPC 契约类型化**（M）
  `shared/ipc.ts` 增加 `IpcContract` 映射（每通道 req/res 类型），preload 泛型 `call<C extends keyof IpcContract>`，消灭全部 IPC `as` 断言与"注释即契约"。

- [ ] **A3. Minio 客户端单例 + 重试策略**（M）
  模块级单例（配置身份变更时失效，`ipc.ts` 已有 identity 检查可作钩子）；upload/download/HEAD 包一层 3 次指数退避重试。
  ~~`removeObjects` 检查逐项响应错误~~ ✅ 已随 P0 修复完成（`health.ts cleanupOrphanObjects`：Quiet 模式仅返回失败项，逐条读取 `Error.Key` 计数并从白名单剔除）。

- [ ] **A4. sharp/sha1 迁入 `utilityProcess`/worker 池**（L）
  缩略图/预览/填充预渲染/裁剪/哈希全部离开主进程，导入与完整性校验期间 IPC 与 `media://` 保持响应。首选试点：`verifyIntegrity`（带进度流）。

- [ ] **A5. `library.json` → `better-sqlite3`**（L）
  根治全量重写模式；顺带获得 id 索引（消除 `media.ts`/`slideshow.ts`/`engine.ts` 的线性 `find`）。小配置类存储保留 JSON。
